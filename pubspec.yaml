name: wheretosleepinnju
description: wheretosleepinnju

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 4.0.2+67

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  shared_preferences: 
    path: ./external/flutter_packages/packages/shared_preferences/shared_preferences
  webview_flutter: 
    path: ./external/flutter_packages/packages/webview_flutter/webview_flutter
  path_provider:
    path: ./external/flutter_packages/packages/path_provider/path_provider
  url_launcher: 
    path: ./external/flutter_packages/packages/url_launcher/url_launcher
  image_picker:
    path: ./external/flutter_packages/packages/image_picker/image_picker
  palette_generator:
    path: ./external/flutter_packages/packages/palette_generator
  mobile_scanner:
    path: ./external/fluttertpc_mobile_scanner

  sqflite: ^2.0.0+4
  flutter_swiper_null_safety_flutter3: ^2.0.0
  scoped_model: ^2.0.0-nullsafety.0
  azlistview: ^2.0.0-nullsafety.0
  flutter_native_splash: ^2.0.5
  flutter_linkify: ^6.0.0
  flutter_html: ^3.0.0-beta.2
  qr_flutter: ^4.0.0
  zxing2: ^0.2.3
  archive: ^3.4.10
  html: ^0.15.0
  dio: ^5.9.0
  # 只用在 Android，请求忽略电池优化，降低厂商定制系统清后台导致登录
  # 会话丢失的概率；iOS/鸿蒙没有这个概念，调用处会用 Platform.isAndroid 判断跳过。
  permission_handler: ^11.3.1
  # 课程自定义配色用的取色器（HSV 色轮 + 预设色块）。纯 Dart 实现，不含
  # 任何平台原生代码/platform channel，鸿蒙 OHOS 构建不需要额外适配。
  flutter_colorpicker: ^1.1.0

  # 后台定时同步课表的调度器。插件只声明了 android / ios 两个平台实现，
  # 鸿蒙上不会被注册——所以那边**能编译，但一调用就是 MissingPluginException**。
  # 全部调用都收口在 BackgroundSyncScheduler 里，由它按平台拦掉，鸿蒙降级为
  # 用户手动点「更新当前课程表」。将来鸿蒙要自动更新得自写原生调度。
  workmanager: ^0.5.2

  # 后台同步完成后的本地通知。跟 workmanager 一样只有 android / ios 实现，
  # 鸿蒙上调用会抛 MissingPluginException——所有调用收口在 SyncNotifier 里
  # 由它按平台拦掉。iOS 也暂时关着，理由与调度器一致（见 SyncNotifier）。
  flutter_local_notifications: ^17.2.4

  # https://gitcode.com/openharmony-sig/flutter_plus_plugins/pull/52
  package_info_plus:
    path: ./external/flutter_plus_plugins_package_info_plus/packages/package_info_plus/package_info_plus
  # 只用在iOS，等待鸿蒙支持
  in_app_review: ^2.0.3

  umeng_common_sdk: ^1.2.7
  umeng_common_sdk_ohos: 1.2.9
  device_calendar: 4.3.3
  device_calendar_ohos:
    path: ./external/fluttertpc_device_calendar/ohos

  # https://gitcode.com/openharmony-sig/fluttertpc_share_extend/pull/8
  share_extend:
    git:
      url: https://gitcode.com/fuchuanyang/fluttertpc_share_extend.git
  fluttertoast:
    git:
      url: https://gitcode.com/openharmony-sig/flutter_fluttertoast.git


dependency_overrides:
  collection: ^1.19.0
  scrollable_positioned_list: 0.3.2
  url_launcher:
    path: ./external/flutter_packages/packages/url_launcher/url_launcher
  shared_preferences:
    path: ./external/flutter_packages/packages/shared_preferences/shared_preferences
  webview_flutter:
    path: ./external/flutter_packages/packages/webview_flutter/webview_flutter
  path_provider:
    path: ./external/flutter_packages/packages/path_provider/path_provider
  image_picker:
    path: ./external/flutter_packages/packages/image_picker/image_picker
  sqflite:
    path: ./external/flutter_sqflite/sqflite
  sqflite_common:
    path: ./external/flutter_sqflite/sqflite_common

dev_dependencies:
  flutter_lints: ^1.0.4
  flutter_test:
    sdk: flutter
  intl_utils: ^2.8.7

flutter:
  uses-material-design: true
  assets:
    - res/icon.png
    # 南大统一身份认证的自动登录脚本（原 Chrome 扩展 content script），
    # 由 NjuAutoLoginScript 读出来注入登录页的 WebView。
    - assets/scripts/auto_auth_login.js
    # 脚本过滑块验证时回放的真人拖动轨迹。
    - assets/recordings/3.json

flutter_intl:
  enabled: true

analyzer:
  strong-mode: true
  language:
    enableSuperMixins: true

flutter_native_splash:
  color: "#ffffff"
  color_dark: "#000000"
